services:
  - type: web
    name: ipo-friends-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: UPSTOX_ACCESS_TOKEN
        sync: false
      - key: CACHE_TTL_MS
        value: "60000"
